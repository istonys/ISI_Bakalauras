experiment:
  dataset_name: voicebank
  train_manifest: /scratch/lustre/home/${USER}/data/voicebank_28spk_manifest/train_manifest.csv
  val_manifest: /scratch/lustre/home/${USER}/data/voicebank_28spk_manifest/val_manifest.csv
  test_manifest: /scratch/lustre/home/${USER}/data/voicebank_28spk_manifest/test_manifest.csv
  model_name: UNetDilatedMaskCNN
  loss_name: L2
  seed: 42
  epochs: 200
  batch_size: 256
  learning_rate: 0.001
  target_sample_rate: 16000
  n_fft: 1024
  hop_length: 256
  win_length: 1024
  segment_seconds: 2.0
  subset_fraction: 1.0
  max_files_to_eval: null
  save_example_count: 3
  num_workers: 10
  early_stopping: true
  patience: 10
  min_delta: 0.0001
  compute_train_eval_loss: true
  compute_test_loss: true
  eval_mode: full_chunks
  results_dir: results
  checkpoints_dir: checkpoints
  device: auto
  use_amp: true
  use_lr_scheduler: true
  lr_scheduler_patience: 3
  lr_scheduler_factor: 0.5
  grad_clip_norm: 5.0
  eval_every_n_epochs: 5
  experiment_id: full_voicebank_unetdilatedmaskcnn_v2
